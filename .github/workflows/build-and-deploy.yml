# When a push occurs on main run checkout, install node and dependencies, run tests, run lint-staged, run next build and copy to artifact
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 20

      - name: Install dependencies
        run: yarn install

      - name: Run tests
        run: yarn test

      - name: Run lint-staged
        run: yarn lint-staged

      - name: Build
        run: yarn build

      - name: Archive production artifact
        uses: actions/upload-artifact@v2
        with:
          name: production
          path: out

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Download production artifact
        uses: actions/download-artifact@v2
        with:
          name: production

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./out